# =============================================================
# CONFIGURATION FRONTEND - DevOps Monitoring Hub
# =============================================================
# Copiez ce fichier en .env (dev) ou .env.production (build prod)
#
#   cp .env.example .env              # pour le dev local
#   cp .env.example .env.production   # pour le build production
#
# IMPORTANT : VITE_API_URL est injecté au BUILD TIME par Vite.
# Si vous changez cette valeur, vous devez REBUILDER le frontend.
# =============================================================

# URL du backend API (OBLIGATOIRE en production)
# Dev local : http://localhost:3001/api
# Production : https://votre-backend.example.com/api
VITE_API_URL=http://localhost:3001/api
